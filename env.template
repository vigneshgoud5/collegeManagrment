# ============================================
# Docker Compose Production Environment
# ============================================
# Copy this file to .env and update with your production values
# Command: cp env.template .env
# NEVER commit .env to version control

# ============================================
# Server Configuration
# ============================================
NODE_ENV=production
SERVER_PORT=3000

# ============================================
# MongoDB Configuration
# ============================================
# For Docker Compose (internal network):
MONGO_URI=mongodb://mongo:27017/college_portal

# For MongoDB Atlas (cloud):
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/college_portal?retryWrites=true&w=majority

# MongoDB Root Credentials (for Docker Compose)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change_me_strong_password_here

# ============================================
# JWT Secrets (REQUIRED - Generate strong random strings)
# ============================================
# Generate secrets with:
#   openssl rand -base64 32
# Or use:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_ACCESS_SECRET=your_super_secret_access_token_key_change_me
JWT_REFRESH_SECRET=your_super_secret_refresh_token_key_change_me

# ============================================
# Client Configuration
# ============================================
# Frontend URL for CORS (your domain or IP)
# For production, use your actual domain:
CLIENT_ORIGIN=http://localhost

# API Base URL for client build (must match your backend URL)
# For production, use your actual backend URL:
VITE_API_BASE_URL=http://localhost:3000/api

# Client Port (for Docker)
CLIENT_PORT=80

# ============================================
# Notes
# ============================================
# 1. Change all default passwords and secrets
# 2. Use strong, random strings for JWT secrets (32+ characters)
# 3. Update CLIENT_ORIGIN and VITE_API_BASE_URL with your actual domain
# 4. For HTTPS, use https:// in CLIENT_ORIGIN and VITE_API_BASE_URL
# 5. Keep this file secure and never commit it to version control

